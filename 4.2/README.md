# SIT725 4.2P – Server + DB + Seed

## Requirements
- Node.js installed
- MongoDB running locally (or use MongoDB Atlas)

## Setup
### 1) Server
```bash
cd server
npm install
cp .env.example .env
npm run seed
npm run dev
